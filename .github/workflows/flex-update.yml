name: Update Flex endpoint

on:
    push:
        branches:
            - main

defaults:
    run:
        shell: bash

jobs:
    call-flex-update:
        uses: symfony-php-recipes/symfony-recipes-php/.github/workflows/callable-flex-update.yml@main
        with:
            versions_json: .github/versions.json
